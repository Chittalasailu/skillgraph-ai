# Root environment example for SkillGraph AI
# Backend variables live in backend/.env.example
# Frontend variables live in frontend/.env.example

# Copy backend/.env.example to backend/.env and frontend/.env.example to frontend/.env as needed.
